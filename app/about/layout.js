export const metadata = {
  title: 'About - nok',
  description: 'Learn how nok is transforming returns from a cost center into a revenue driver for modern brands',
}

export default function AboutLayout({ children }) {
  return children
}

